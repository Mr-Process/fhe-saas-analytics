# Python package dependencies for the FHE SaaS analytics project

# Pyfhel provides Python bindings for the Microsoft SEAL library.  It enables
# us to generate encryption keys and perform homomorphic operations on
# ciphertexts.  See docs/research_fhe.md for more information on FHE libraries.
Pyfhel>=3.4.1

# Flask can be used to implement a simple REST API for sending encrypted data
# from clients to the server evaluator.  It is optional and can be replaced
# with any web framework.
Flask>=3.0
